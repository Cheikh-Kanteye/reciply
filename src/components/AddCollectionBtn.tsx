import { StyleSheet, Text, View } from "react-native";
import React from "react";

const AddCollectionBtn = () => {
  return (
    <View>
      <Text>AddCollectionBtn</Text>
    </View>
  );
};

export default AddCollectionBtn;

const styles = StyleSheet.create({});
